This uses the http server to create an higher level component that lets register request endpoints

To extend this component, add new endpoint in the config struct and define its handler in the src file