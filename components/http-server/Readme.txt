This is created as a pure decoupled (from implementation) server
But actually its purpose is to implement the input and outpiut interfaces which weere implemented earlier 
by button and printfs
So an adapter component will use it to provide those interfaces through it